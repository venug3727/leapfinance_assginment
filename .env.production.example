# =============================================================================
# ENVIRONMENT VARIABLES TEMPLATE FOR PRODUCTION
# =============================================================================
# Copy this file to .env.production and fill in your values
# DO NOT commit .env.production to git!
# =============================================================================

# MongoDB Atlas Connection String
# Get this from: MongoDB Atlas > Connect > Connect your application
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/infopulse?retryWrites=true&w=majority

# JWT Secret Key (generate a strong random string)
# Use: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-min-32-characters

# Service API Key (for service-to-service auth)
SERVICE_API_KEY=your-service-api-key-here

# CORS Allowed Origins (comma-separated)
# Add your production frontend URLs
CORS_ALLOWED_ORIGINS=https://your-dashboard-url.railway.app,https://your-custom-domain.com

# Backend URL (for dashboard to connect)
NEXT_PUBLIC_API_URL=https://your-collector-url.railway.app

# Internal collector URL (for sample service)
COLLECTOR_URL=http://collector-service:8080

# Ports (defaults work for most deployments)
COLLECTOR_PORT=8080
SAMPLE_PORT=8081
DASHBOARD_PORT=3000

# Logging Level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO
